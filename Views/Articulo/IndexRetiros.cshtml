@model IEnumerable<TestProyecto2.Models.ArticulosRetirados> //establece sobre que modelo se esta trabajando

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container"> <!--Construccion de la vista con bootstrap que viene instalado por defecto en asp.net core MMVC-->

    <div class="row">
        <div class="col-sm-6">
            <h3>Lista de Articulos retirados</h3>
        </div>
        <div class="col-sm-6">
            <a class="btn btn-success d-block" asp-action="RegistrarRetiro" asp-controller="ArticuloController">Retirar Articulo</a>
        </div>

    </div>

    <div class="row">
            @if (Model.Count() > 0) //comprueba si hay registros
            {
                <table class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <!--m es una expresion lambda-->
                            <td>@Html.DisplayNameFor(m => m.CodigoCliente)</td>
                            <td>@Html.DisplayNameFor(m => m.TrackingId)</td>
                            <td>@Html.DisplayNameFor(m => m.Descripcion)</td>
                            <td>@Html.DisplayNameFor(m => m.CodigoCliente)</td>
                            <td>@Html.DisplayNameFor(m => m.FechaRetiro)</td>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.CodigoCliente</td>
                                <td>@item.TrackingId</td>
                                <td>@item.Descripcion</td>
                                <td>@item.CodigoCliente</td>
                                <td>@item.FechaRetiro</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else 
            {
                <p>No existen datos</p>
            }
        </div>

</div>